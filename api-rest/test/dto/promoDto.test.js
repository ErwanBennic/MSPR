var assert = require('assert');

describe('check promoDto integrity', function () {
   this.beforeEach(() => {
      
   })
   
   it('should return true if promo is valid', function () {
      assert.equal("Hello".length, 0);
   });

});